# Styles

Diretorio reservado para estilos compartilhados. Nesta etapa:

- estilos globais minimos ficam em `src/app/globals.css`;
- tokens de design compartilhados ficam em `src/lib/design/tokens.ts`.
